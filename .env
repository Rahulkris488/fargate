AWS_REGION=ap-southeast-2
QDRANT_URL=http://localhost:6333      # local Qdrant or SSH tunnel (see below)
BEDROCK_LLM_MODEL=anthropic.claude-3   # example, set in ECS later
BEDROCK_EMBED_MODEL=amazon.titan-embed-text-v1